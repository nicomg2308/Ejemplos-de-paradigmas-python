#dada una clase que tiene dos responsabilidades
import msvcrt
Clase Rectangulo:
        def _init_ (self, width=0, height=0):
            self.width =width
            self.height=height
        def draw (self):
            #Hacer un poco de dibujo
            pass
        def area (self):
            return self.width*self.height
msvcrt.getch()
